const { injectPlugin, filterPlugin, sortPlugin } = require('./util/plugin');
const rewriter = require('./util/rewriter');

module.exports = {
  injectPlugin,
  filterPlugin,
  sortPlugin,
  rewriter
};
